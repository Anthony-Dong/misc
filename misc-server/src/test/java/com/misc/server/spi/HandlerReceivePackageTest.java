package com.misc.server.spi;

import org.junit.Test;

import static org.junit.Assert.*;

public class HandlerReceivePackageTest {

    @Test
    public void buildRequest() {
//        HashMap<String, String> map = new HashMap<>();
//        map.put("name", "xiaoli");
//        URL http = new URL("http", "127.0.0.1", 6379, map);
//        String decode = URL.decode(http.toString());
//        byte[] body = new byte[10];
//        MiscPack pack = new MiscPack(decode, body);
//        Request request = HandlerReceivePackage.buildRequest(pack);
//        pack.release();
//        System.out.println(request.getBody().length);
    }
}